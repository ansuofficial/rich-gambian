export type CategoryDataType = {
  name: string;
  value: number;
};

export interface ProductsTypes {
  id: number;
  name: string;
  price: number;
  category: string;
  emoji: string;
  image: string;
}
